// Example runtime config for injecting environment values into the static site.
// Copy this to `html/config.js` and update the values. Do NOT commit `config.js`.

window.__ENV__ = window.__ENV__ || {};
window.__ENV__.CORRECT_CODE = "PEACH2025"; // replace with your real code
